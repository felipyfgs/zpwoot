-- Migration: add_indexes
-- Additional indexes for performance optimization
-- Note: Most indexes are already created in 001_initial_schema.up.sql

-- This migration is kept for future additional indexes if needed
-- All primary indexes are already created in the initial schema
